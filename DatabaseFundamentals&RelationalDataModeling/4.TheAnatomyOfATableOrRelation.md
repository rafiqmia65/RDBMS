# The Anatomy of a Table or Relation

## 📌 Introduction

In a **Relational Database Management System (RDBMS)**, data is stored in **tables**, also called **relations**.  
Each table represents a real-world entity and follows a strict structure that ensures consistency, integrity, and efficient querying.

This document explains the **complete anatomy of a table/relation**—its components, purpose, and how they work together.

---

# 📘 What Is a Table (Relation)?

A **table (relation)** is a structured collection of data organized into rows and columns.

- **Row = tuple (record)**
- **Column = attribute (field)**
- **Table name = entity**
- **Primary key = unique identifier**

Every table describes a single entity type, such as `Users`, `Products`, `Orders`, or `Students`.

---

# 🧱 Anatomy of a Table / Relation

A relational table includes the following components:

1. **Table Name**
2. **Attributes (Columns)**
3. **Domain (Data Types)**
4. **Tuples (Rows / Records)**
5. **Primary Key**
6. **Candidate Keys**
7. **Alternate Keys**
8. **Foreign Key**
9. **Composite Key**
10. **Constraints**
11. **Null Values**
12. **Metadata**

Each part is described below.

---

# 1️⃣ Table Name

### ✔ Description

A table has a unique name within a database.

### ✔ Purpose

- Represents an entity (e.g., `Employees`, `Orders`, `Students`)
- Used for referencing in queries

---

# 2️⃣ Attributes (Columns)

### ✔ Description

Attributes are the **fields** of a table.  
Example: In a `Students` table, common attributes are:

- `student_id`
- `name`
- `email`
- `age`

### ✔ Purpose

- Represent the properties of the entity
- Define what type of information the table stores

---

# 3️⃣ Domain (Data Types)

### ✔ Description

Each attribute has a **domain**, meaning the set of valid values it can hold.

### Examples:

- `INT`
- `VARCHAR(100)`
- `DATE`
- `BOOLEAN`

### ✔ Purpose

- Prevents invalid data entry
- Maintains consistency across the table

---

# 4️⃣ Tuples (Rows / Records)

### ✔ Description

Each row represents a single record or instance of the entity.

### Example:

In a `Users` table, a tuple might be:

| id  | name  | email           |
| --- | ----- | --------------- |
| 1   | Rafiq | rafiq@email.com |

### ✔ Purpose

Stores actual data inside the table.

---

# 5️⃣ Primary Key (PK)

### ✔ Description

A primary key **uniquely identifies each row**.

### Properties:

- Must be unique
- Cannot be NULL
- Only one primary key per table

### Example:

- `student_id`
- `product_id`

### ✔ Purpose

Ensures each record is uniquely identifiable.

---

# 6️⃣ Candidate Keys

### ✔ Description

All attributes that can uniquely identify a record.

### Example:

In a `Users` table:

- `email`
- `user_id`

Both can be unique → Both are candidate keys.

---

# 7️⃣ Alternate Keys

### ✔ Description

Candidate keys **not chosen** as the primary key.

### Example:

If primary key = `user_id`,  
Alternate key = `email`.

---

# 8️⃣ Foreign Key (FK)

### ✔ Description

A foreign key is an attribute in one table that **refers to the primary key of another table**.

### Example:

`orders.user_id` → references → `users.id`

### ✔ Purpose

- Maintain relationships
- Enforce referential integrity

---

# 9️⃣ Composite Key

### ✔ Description

A primary key made of **two or more attributes**.

### Example:

`Enrollment(student_id, course_id)`

### ✔ Purpose

Used when a single attribute cannot uniquely identify a record.

---

# 🔟 Constraints

Constraints enforce rules on the data inside the table.

### Common Constraints:

- **PRIMARY KEY** – Unique & Not Null
- **UNIQUE** – Prevents duplicate values
- **NOT NULL** – Value cannot be empty
- **CHECK** – Ensures valid values (`age > 18`)
- **DEFAULT** – Assigns default values
- **FOREIGN KEY** – Enforces relationships

### ✔ Purpose

Keeps the data clean, consistent, and reliable.

---

# 1️⃣1️⃣ Null Values

### ✔ Description

NULL represents **unknown / missing / not applicable** data.

### ✔ Notes

- NULL is not zero
- NULL is not an empty string
- Cannot be used in comparison directly

---

# 1️⃣2️⃣ Metadata

Metadata means **data about data**.

### Includes:

- Table name
- Column names
- Data types
- Indexes
- Constraints
- Relationships

### ✔ Purpose

Defines structure, helps query optimization, and maintains database integrity.

---

# 📐 Example Table Structure

A simple `Students` table:

| student_id (PK) | name  | email (UNIQUE) | age | course_id (FK) |
| --------------- | ----- | -------------- | --- | -------------- |
| 1               | Rafiq | rafiq@mail.com | 22  | 2              |

This table contains:

- Primary key: `student_id`
- Alternate key: `email`
- Foreign key: `course_id`
- Attributes with domain values
- Tuples (rows)
- Constraints enforcing validity

---

# 📝 Final Summary

A table (relation) consists of:

- **Name**
- **Attributes (Columns)**
- **Domains (Data Types)**
- **Tuples (Rows)**
- **Keys (Primary, Candidate, Alternate, Foreign, Composite)**
- **Constraints (Not Null, Unique, Check, etc.)**
- **Null values**
- **Metadata**

These components define how data is stored, validated, and related to other tables inside a relational database.

---

## © License

This document is free to use, modify, and include in your GitHub repository.
